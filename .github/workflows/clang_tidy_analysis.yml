# *******************************************************************************
# Copyright (c) 2026 Contributors to the Eclipse Foundation
#
# See the NOTICE file(s) distributed with this work for additional
# information regarding copyright ownership.
#
# This program and the accompanying materials are made available under the
# terms of the Apache License Version 2.0 which is available at
# https://www.apache.org/licenses/LICENSE-2.0
#
# SPDX-License-Identifier: Apache-2.0
# *******************************************************************************

name: Clang-Tidy Analysis

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, reopened, synchronize]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: clang_tidy_analysis-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  ANDROID_HOME: ""
  ANDROID_SDK_ROOT: ""

jobs:
  clang_tidy:
    runs-on: ubuntu-24.04
    steps:
      - name: Start timer
        id: start_time
        run: echo "start=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Free Disk Space (Ubuntu)
        uses: eclipse-score/more-disk-space@v1
        with:
          level: 4

      - uses: bazel-contrib/setup-bazel@0.18.0
        with:
          bazelisk-cache: true
          disk-cache: "clang_tidy_analysis"
          disk-cache-key: "main"
          repository-cache: true

      - name: Note cache context
        run: |
          if [[ "${GITHUB_REF}" == "refs/heads/main" ]]; then
            echo "Cache seed run on main branch; subsequent runs should restore cache." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "Cache consume run; the first run after cache-key or workflow changes may miss restore once." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Allow linux-sandbox
        uses: ./actions/unblock_user_namespace_for_linux_sandbox

      - name: Run clang-tidy analysis
        run: |
          bazel build //... --aspects=//:tools/lint/linters.bzl%clang_tidy_aspect

      - name: End timer
        if: ${{ always() }}
        id: timer
        run: |
          end=$(date +%s)
          start=${{ steps.start_time.outputs.start }}
          duration=$((end - start))
          echo "duration-seconds=$duration" >> "$GITHUB_OUTPUT"
          echo "clang-tidy duration: ${duration}s" >> "$GITHUB_STEP_SUMMARY"
